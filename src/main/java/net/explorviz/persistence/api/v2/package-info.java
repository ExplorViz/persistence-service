/**
 * Ths package contains REST APIs for the now deprecated v2 API of the ExplorViz frontend
 * which uses nested structures as opposed to a flat data model. It is kept here to ensure
 * compatibility with old versions of the ExplorViz frontend. For implementing new features,
 * refer to the newer versions instead.
 */
package net.explorviz.persistence.api.v2;
